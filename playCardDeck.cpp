#include "playCardDeck.h"
#include <stdio.h>
#include "cards.h"
int playCardDeck::doStuff() {
    return 1;
}

void playCardDeck::generateCards() {
    deck -> 


    for (int i = 0; i < 52; ++i) {
        printf("%d\n", i);
    }
}